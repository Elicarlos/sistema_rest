# from django.db import models
# from perfil.models import Perfil


""" class Usuario(models.Model):
    nome = models.CharField(max_length=30)
    email = models.EmailField()
    perfil = models.ForeignKey(Perfil, on_delete=models.CASCADE)
    usuario = models.CharField(max_length=50)
    senha = models.CharField(max_length=30)

    '''Definir loja de acesso


    # """ 
    # [] Permitir que este usuario seja selecionado como vendedor durante uma venda
    # [] Permitir que usuario seja selecionado como técnico em ordem de servico
    # [] Este usuario não terá acesso ao sistema
      
    # """ """